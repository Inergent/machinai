node_modules/

# Anchored deliberately. A bare `build/` or `dist/` matches any directory of
# that name at any depth — it silently swallowed apps/web/app/api/build/, so
# the Build endpoint was never committed and 404'd in production.
/dist/
/build/
/out/

.next/
.turbo/
*.tsbuildinfo
next-env.d.ts

.env
.env*.local
.vercel/

# Harness working checkouts
.machinai-work/

.DS_Store
Thumbs.db
